spring.application.name=medapp
# Datasource
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/MedApp}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:@Exprivia.1}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Forza il percorso di Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
# Abilita l'interfaccia
springdoc.swagger-ui.enabled=true

spring.mail.host=smtp.ethereal.email
spring.mail.port=587
spring.mail.username=jayson49@ethereal.email
spring.mail.password=3askvAAXSG1wgeN6W3
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Impedisce la creazione dell'oggetto con hour/minute/second
spring.jackson.serialization.write-dates-as-timestamps=false